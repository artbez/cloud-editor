spring:
  cloud:
    config:
      name: gateway
      discovery:
        service-id: config
        enabled: true
  devtools:
    restart:
      enabled: true
      exclude: iimetra/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/