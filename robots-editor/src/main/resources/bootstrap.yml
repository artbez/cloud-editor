spring:
  application:
    name: robotseditor
  cloud:
    config:
      name: robotseditor
      discovery:
        service-id: config
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/