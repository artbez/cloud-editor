spring:
  application:
    name: logger
  cloud:
    config:
      name: logger
      discovery:
        service-id: config
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/