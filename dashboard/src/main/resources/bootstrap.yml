spring:
  application:
    name: dashboard
  cloud:
    config:
      name: dashboard
      discovery:
        service-id: config
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/