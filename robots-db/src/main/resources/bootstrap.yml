spring:
  application:
    name: robotsdb
  cloud:
    config:
      name: robotsdb
      discovery:
        service-id: config
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/